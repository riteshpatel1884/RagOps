import PhasePlaceholder from "@/components/PhasePlaceholder";

export default function Page() {
  return (
    <PhasePlaceholder
      title="Experiments"
      phase="Phase 5 — Automated Experiment Engine"
      description="Sweep pipeline configurations and run them against an evaluation dataset automatically."
    />
  );
}
